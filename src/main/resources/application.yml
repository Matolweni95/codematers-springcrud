spring:
  datasource:
    url: jdbc:mysql://localhost:3306/codemaster
    username: root
    password: "@tanaLoLo33"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
      database: mysql
      database-platform: org.hibernate.dialect.MySQLDialect

  mail:
    host: smtp.gmail.com
    port: 465
    username: codemasters2023@gmail.com
    password: dyxx llmv iwpj ncpn
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
